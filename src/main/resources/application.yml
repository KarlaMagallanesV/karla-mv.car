server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: ms-vehiculos
  datasource:
    url: jdbc:postgresql://${DB_HOST:ep-proud-frost-at2hkg9m-pooler.c-9.us-east-1.aws.neon.tech}/${DB_NAME:vehiculos}?sslmode=${DB_SSL_MODE:require}&channel_binding=require
    username: ${DB_USER:neondb_owner}
    password: ${DB_PASSWORD:npg_JVbPl1cSs4xA}
    driver-class-name: org.postgresql.Driver
    hikari:
      enabled: true
      initial-size: 1
      max-size: 5
      max-idle-time: 10m
      validation-query: SELECT 1
